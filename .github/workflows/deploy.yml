name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Copy files to server via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          source: "."
          target: "/srv/projects/livelink"
          rm: false

      - name: Deploy with SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd /srv/projects/livelink
              
            # Forcer la production
            export NODE_ENV=production
            
            # Vérifier que DATABASE_URL est configuré
            if [ -z "$DATABASE_URL" ]; then
              echo "⚠️ ATTENTION: DATABASE_URL non configuré en production!"
              echo "🔧 Tentative de configuration automatique..."
              
              # Essayer de récupérer depuis docker-compose
              export DATABASE_URL="postgresql://liveshop_user:motdepassefort@fitsen-postgresql:5432/liveshop"
              echo "🔗 DATABASE_URL configuré: ${DATABASE_URL:0:20}..."
            fi
            
            echo "🚀 Déploiement en mode PRODUCTION"
            echo "📊 Base de données: PostgreSQL"
            echo "🔗 DATABASE_URL: ${DATABASE_URL:0:20}..."
            
            # Déployer avec rebuild forcé
            docker compose down
            docker compose pull
            docker compose up -d --build --force-recreate
            
            echo "✅ Déploiement terminé!"
            echo "🔍 Vérification des services..."
            docker compose ps
            
            # Attendre que le backend soit prêt
            echo "⏳ Attente du démarrage du backend..."
            sleep 10
            
            # Migration des commentaires
            echo "🔧 Migration des commentaires..."
            docker compose exec -T backend node src/scripts/migrate-production.js || {
              echo "⚠️ Migration des commentaires échouée, vérification manuelle..."
              docker compose exec -T backend node -e "
                const { sequelize } = require('./src/config/database');
                sequelize.query('SELECT table_name FROM information_schema.tables WHERE table_name = \\'comments\\'').then(([rows]) => {
                  console.log('Tables comments:', rows);
                  process.exit(0);
                }).catch(err => {
                  console.error('Erreur:', err);
                  process.exit(1);
                });
              "
            }
            
            # Exécuter le seeding automatique
            echo "🌱 Démarrage du seeding automatique..."
            docker compose exec -T backend node src/scripts/seed-production.js || {
              echo "⚠️ Seeding automatique échoué, tentative manuelle..."
              docker compose exec -T backend node -e "
                console.log('🌱 Seed manuel...');
                require('./src/scripts/seed-production.js').seedProduction();
              "
            }
            
            echo "🎉 Déploiement, seeding et migration terminés avec succès!"
